services:
  jackson:
    image: ghcr.io/monokaijs/jackson:${JACKSON_IMAGE_TAG:-latest}
    restart: unless-stopped
    env_file: .env
    volumes:
      - jackson-data:/app/data

volumes:
  jackson-data:
